/*
if (c = '\')
{
	if (next == '\' || // i just realized, '\' might be handled
		next == '%' || // automatically by gcc or c so this all might be unnecessary
		next == '"' ||
		next == 'n' ||
		next == 't')
}
*/
